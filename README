The stable_springer.xenconfig file is a kiwi test image for the xen hypervisor the "description" for this image is based upon the configurations for a pxe boot server the packages installed in this kiwi image can be found in the config.xml file. Also added is the virtual disk used for the kiwi image and a log of the build process used for further study of the stable_springer.xenconfig image 
